//java script help
//string
var name = "scott";
//number
var number = 4;
// one = assigns value to variable; 2 starts to check; 3 checks value and type--dont use 2 usually
if (number === 4) {
  console.log(name);
}
else {
  console.log("not 4");
}
var multiplication = function (x,y) {
  return x * y;
}

var mathStuff = function (x,y) {
  var product = multiplication (x, y);

//array = easy way to gatehr together a bunch of info
}
var names = ['gillet', 'rachel', 'bremen', 'lydia', 'ethan'];
//regular brackets shows it's an array--'' shows it's a string; arrays holds tray of lots of information
for (var i = 0; i < array.length; i++) {
array[i]
}
